.terraform/
*.tfstate
*.tfstate.*
# terraform.tfvars SI se sube a git a proposito: solo tiene valores no sensibles.
# agentboard-ui no tiene variables sensibles (no usa DB ni JWT_SECRET directamente),
# pero se deja la misma convencion que agentboard-api por consistencia.
*.auto.tfvars
crash.log
